import type { AppOverrideSeed } from "../lib/types";

/**
 * Manual overrides for apps where F-Droid metadata is insufficient.
 * Keyed by package name. Only specified fields override F-Droid data.
 */
export const appOverrides: AppOverrideSeed[] = [];
